{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "node dist/seed.js && node dist/seedBlots.js && node dist/index.js",
    "healthcheckPath": "/api/stats",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 3
  }
}
